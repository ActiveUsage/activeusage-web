# Alias loader so `require "activeusage/web"` (matching the gem name) works
# in addition to the standard `require "active_usage/web"` (matching the namespace).
require_relative "../active_usage/web"
