<% content_for :title, "New Version" %>

<div class="au-page-header">
  <div>
    <h1 class="au-page-title">New Version</h1>
    <p class="au-page-sub">Creates a new configuration version — it becomes active immediately.</p>
  </div>
</div>

<div class="au-card">
  <%= render "form", configuration: @configuration %>
</div>
